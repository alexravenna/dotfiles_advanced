alias lt="ls -lrat"
alias cl="clear"
alias ..="cd .."
alias t="top"
alias cll="clear && ll"

alias ea="vim ~/.bash_aliases && source ~/.bash_aliases"
alias chrome="google-chrome &"

mkcd ()
{
  mkdir -p -- "$1" && cd -P -- "$1"
}
